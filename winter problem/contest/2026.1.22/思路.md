
$$
\def\OO{\mathcal O}
\def\opn#1{\operatorname{#1}}
$$

## pingpong

多次询问是假的，直接离线排序。首先 $\sum freq\le 10^6$ 是好做的，只要花费 $\OO(\log n)$ 的时间消掉一个网即可。正解考虑优化这个过程，首先会处理左右端点有多个相同位置的网的情况，直接可以 $\OO(1)$ 消掉一个位置所有的网，然后每次就可以花费 $\OO(\log n)$ 让 $T\gets T-dis,dis\gets dis+1$，所以只会有 $\OO(\sqrt T)$ 种坐标，将所有完全相同的网合并后，每种网只会出现 $\OO(\frac{\sqrt{T}}{len_i})$ 次，所以总和为 $\OO(\sqrt T\ln \sqrt T)$，复杂度是 $\OO((N+\sqrt T)(\log N+\ln \sqrt T)+Q\log Q)$。

